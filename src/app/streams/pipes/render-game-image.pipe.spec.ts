import { RenderGameImagePipe } from './render-game-image.pipe';

describe('RenderGameImagePipe', () => {
  it('create an instance', () => {
    const pipe = new RenderGameImagePipe();
    expect(pipe).toBeTruthy();
  });
});
