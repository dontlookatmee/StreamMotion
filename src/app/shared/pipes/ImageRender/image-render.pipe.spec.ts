import { ImageRenderPipe } from './image-render.pipe';

describe('ImageRenderPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageRenderPipe();
    expect(pipe).toBeTruthy();
  });
});
